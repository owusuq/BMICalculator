"""
admin.py

Django ships with a ready-made admin website (available at /admin/) for viewing
and editing database records. To make one of our models show up there, we would
register it in this file with admin.site.register(ModelName).

Because this app defines no models (see models.py), there is nothing to
register, so this file is left as the default placeholder.
"""

from django.contrib import admin

# Register your models here.
